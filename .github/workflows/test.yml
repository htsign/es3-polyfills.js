name: Unittest

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: setup node
      uses: actions/setup-node@v6
      with:
        node-version: 24.x
    - uses: actions/checkout@v6
    - run: npm ci
    - run: npm test
